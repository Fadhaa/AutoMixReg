% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_data.R
\name{generate_mixture_data}
\alias{generate_mixture_data}
\title{Generate Mixture Regression Data}
\usage{
generate_mixture_data(
  n_samples,
  n_features,
  betas,
  cluster_probs,
  noise_std = 1,
  seed = NULL
)
}
\arguments{
\item{n_samples}{Number of samples to generate}

\item{n_features}{Number of features (excluding intercept)}

\item{betas}{A list of numeric vectors specifying the true regression coefficients for each component.
Each vector should have length = n_features + 1 (intercept included).}

\item{cluster_probs}{Vector of probabilities summing to 1, determining component weights}

\item{noise_std}{Standard deviation of the error term}

\item{seed}{Optional random seed}
}
\value{
A data.frame with generated features (x1, ..., xN), the response variable \code{y}, and the true \code{cluster} label
}
\description{
This function generates synthetic data from a mixture of linear regression models.
You can optionally specify the true regression coefficients (\code{betas}), noise, and cluster probabilities.
}
\examples{
custom_betas <- list(
  c(0.5, 1.5, 2, 3),   # Cluster 1
  c(1, 2, 3, 4),       # Cluster 2
  c(2, 3, 4, 6)        # Cluster 3
)
df <- generate_mixture_data(
  n_samples = 1000,
  n_features = 3,
  betas = custom_betas,
  cluster_probs = c(0.4, 0.3, 0.3),
  noise_std = 1,
  seed = 42
)
head(df)

}
