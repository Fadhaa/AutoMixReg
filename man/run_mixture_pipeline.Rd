% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_model.R
\name{run_mixture_pipeline}
\alias{run_mixture_pipeline}
\title{Run Mixture Regression Model with Automatic Component Selection}
\usage{
run_mixture_pipeline(df, response_col = "y", seed = NULL)
}
\arguments{
\item{df}{Data frame with predictors and response variable.}

\item{response_col}{Name of the response column (default: "y").}

\item{seed}{Optional random seed.}
}
\value{
A list with model, best_k, and bic
}
\description{
This function runs the full mixture regression pipeline:
\itemize{
\item Prepares the data
\item Selects the best number of components using BIC
\item Fits the final model
\item Prints the model summary
}
}
\examples{
custom_betas <- list(
  c(0.5, 1.5, 2, 3),   # Cluster 1
  c(1, 2, 3, 4),       # Cluster 2
  c(2, 3, 4, 6)        # Cluster 3
)
df <- generate_mixture_data(
  n_samples = 1000,
  n_features = 3,
  betas = custom_betas,
  cluster_probs = c(0.4, 0.3, 0.3),
  noise_std = 1,
  seed = 42
)
head(df)

run_mixture_pipeline(df)

}
